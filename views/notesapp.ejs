<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes App</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white">
    <div class="px-15 py-10">
        <form action="/create" method="post" class="flex flex-col gap-3">
        <input class="bg-zinc-800 px-5 py-2 rounded-md w-full" placeholder="enter title here" name="title">
        <textarea class="bg-zinc-800 px-5 py-2 rounded-md w-full" placeholder="enter your notes here" name="notes"></textarea>
        <input type="submit" class="bg-blue-600 rounded-md px-5 py-2 cursor-pointer hover:bg-blue-700" value="create notes">
        </form>
    </div>
    <div class="px-15 flex flex-row flex-wrap gap-4">
        <% if(files.length > 0) { %>
        <% files.forEach((element) => { %>
        <div class="bg-zinc-800 rounded-md py-3 px-6 w-xs">
            <h2 class="font-semibold"><%= element %></h2>
            <a href="/notes/<%= element %>"><p class="font-semibold text-blue-400 text-sm cursor-pointer hover:text-blue-500 mt-1">Read more...</p></a>
            <a href="/edit/<%= element %>"><p class="font-semibold text-gray-400 text-right text-sm cursor-pointer hover:text-gray-500 mt-1">Edit notes</p></a>
        </div>
        <% }) %>
        <% } else { %>
            <p class="font-semibold text-red-500 text-sm cursor-pointer hover:text-red-600 mt-1">No Task found!</p>
        <% } %>
    </div>
</body>
</html>